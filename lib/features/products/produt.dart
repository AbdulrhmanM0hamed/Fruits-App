import 'package:flutter/material.dart';

class ProdutPage extends StatelessWidget {
  const ProdutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child: Text("Product Page"),
      ),
    );
  }
}